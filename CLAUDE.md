# 포트폴리오 자산관리 프로그램

## 프로젝트 개요
- 단일 HTML 파일로 된 개인용 자산관리·포트폴리오 추적 프로그램.
- 여러 증권사 계좌를 집계한다: 미래에셋(종합/ISA/연금저축), 메리츠(국내주식/해외주식), 토스증권.
- 각 계좌의 보유종목, 평단/현재가/손익, 배당금, 전체 합계를 보여준다.

## 핵심 데이터 규칙 (반드시 지킬 것)
- 모든 가격·손익은 원(KRW) 기준, 해외주식만 USD 평단/현재가 표기 후 환율 적용.
- 투자금 = 전체 매수 누적원가(매도된 분 포함). 매도 시 실현손익 += (매도가−평단)×매도수량.
- 예수금(현금) 기능은 제거됨 — 계좌 평가액/투자금/손익만 추적, 현금 잔고는 추적하지 않음.
- 배당 종목명은 정규화해서 집계(타겟/타켓/다켓, TOP10/TOOP10 등 오타 병합) — `normalizeDivName()` 참고.

## 완료된 작업
- [x] 매도 처리: 보유수 차감 + 실현손익 기록.
- [x] 종목 상태 플래그 (보유중 / 청산) — 청산 종목은 보유목록·평가액·투자금에서 제외, 배당 이력은 보존(SOL CD 6,325원 유지).
- [x] 투자금 정의 통일 (전체 매수원가).
- [x] 배당 종목명 정규화 — 타겟/타켓/다켓, TOP10/TOOP10 병합.
- [x] 예수금 기능 추가 후 제거 — 수수료 포함 자동계산이 복잡하고 실익 적어 드롭. 필요 시 재검토.
- [x] 환율 자동조회 복구 — open.er-api.com → frankfurter.app → exchangerate-api.com 순 시도, 24시간 캐시(localStorage), 실패 시 수동값 폴백. 앱 로딩 시 자동 실행 + 새로고침 버튼은 캐시 무시하고 강제 재조회.
- [x] **동기화 데이터 보호 안전장치** (중요) — 구글시트 fetch 실패 + 이 기기 로컬백업도 없으면(`syncEnabled=false`) 빈 상태를 절대 구글시트에 저장하지 않음. 과거 이 가드가 없어서 휴대폰 첫 접속 시 빈 데이터로 전체 데이터가 삭제된 사고가 있었음 (구글시트 버전기록 없이 로컬 PC `localStorage.pfv4Backup`으로 겨우 복구). 환율 자동조회 성공 시 더 이상 자동 `save()` 호출 안 함(이게 사고의 직접 트리거였음).
- [x] **시세조회를 GAS 경유 방식으로 전환** — 야후 파이낸스를 브라우저에서 직접 호출하면 CORS로 100% 차단됨. Apps Script(`GAS_URL`)의 `fetchPrices(kr, us)` 함수가 `UrlFetchApp.fetch()`로 서버에서 대신 조회(User-Agent 헤더 필수, 안 넣으면 야후가 차단). Apps Script에서 `UrlFetchApp` 첫 사용 시 `script.external_request` 권한을 별도로 승인해야 함(테스트 함수 실행 → 권한 팝업 → 허용 → 새 버전으로 재배포 필요). 클라이언트는 `fetchPricesJSONP()`로 GAS를 JSONP 호출.
  - **코스피/코스닥 둘 다 조회해서 최신 시각(`regularMarketTime`) 기준으로 선택** — 야후가 일부 코스닥 종목(레인보우로보틱스, 에코프로비엠, 리가켐바이오, 현대무벡스, 강원에너지 등)의 `.KS` 데이터로 **2024년 7월에 멈춘 죽은 값**을 계속 반환함(에러 없이 200 OK로 응답해서 단순히 "성공하면 그 값 사용" 로직으로는 못 걸러냄). 단순히 `.KS` 먼저 시도 후 실패시 `.KQ`로는 부족 — 반드시 둘 다 조회해서 `regularMarketTime`이 더 큰(최신) 쪽을 채택해야 함.
- [x] 종목코드 없는 종목(펀드 등) 처리 — 코드 없으면 종목명을 식별자로 사용해 동일 계좌 내 여러 무코드 종목이 서로 합쳐지는 버그 해결. HTML 속성에 종목명을 끼워넣을 때는 `escAttr()`로 이스케이프 + `data-` 속성 사용(특수문자로 버튼 깨지는 것 방지).

## 진행할 작업 (TODO)

### 1. 자산 변동 추이 그래프
- 계좌별/전체 잔고(평가액) 변화를 시간 순으로 보여주는 라인 차트
- 매수·매도·배당 이벤트 기준으로 스냅샷을 쌓아 추이 시각화
- 필요 시 요청

### 2. 펀드 입력 UX 확인 필요
- 현재는 전용 폼 없이 일반 매수/매도 폼으로 펀드도 입력 가능 (종목명/코드 직접 입력).
- 현재가는 야후 파이낸스 자동조회가 안 되므로 보유종목 표에서 직접입력 칸으로 수동 입력.
- 사용자 확인 후 전용 UI 필요 여부 결정.

## 참고 메모
- 동기화 구조: 구글시트(GAS_URL, JSONP) 우선, 실패 시 `localStorage.pfv4Backup` 폴백. `syncEnabled` 플래그로 양쪽 다 비었을 때 저장 차단(데이터 보호).
- GAS_URL이 바뀌면 `index.html`의 `const GAS_URL` 상수도 같이 갱신해야 함. Apps Script 코드 수정 시 "배포 → 배포 관리 → 편집 → 새 버전 → 배포"까지 해야 실제 URL에 반영됨(저장만 하면 반영 안 됨).
- 작업 진행하면서 새로 정한 규칙이나 끝낸 항목은 이 파일을 갱신해 둘 것.